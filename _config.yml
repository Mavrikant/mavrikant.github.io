title:              M. Serdar Karaman
email:              serdar@karaman.dev
description:        Aviyonik yazılım, DO-178C ve emniyet kritik mühendislik üzerine sahadan notlar.
author:             M. Serdar Karaman
baseurl:            "/"
url:                "https://karaman.dev"
lang:               tr
locale:             tr_TR

# jekyll-seo-tag — sosyal medya / OG defaults
defaults:
  - scope:
      path: ""
    values:
      image: /img/og-default.webp
      twitter:
        card: summary_large_image

favicon:
  source: favicon.jpg
sass:
  style: compressed
  sourcemap: never

# Social Profiles
twitter_username:
github_username:     Mavrikant
facebook_username:
instagram_username:
linkedin_username: mserdarkaraman
youtube_username: UCIlBH9QynIkHxX8uiY0UEMQ

# Giscus (GitHub Discussions tabanlı yorumlar) — repo_id ve category_id
# doldurulana kadar Disqus kullanılmaya devam eder. Değerler için:
# 1) Repoda Discussions'ı aç, 2) "Blog Comments" kategorisi oluştur
# (Announcements türü), 3) giscus app'i kur (github.com/apps/giscus),
# 4) giscus.app'ten repo_id / category_id değerlerini kopyala.
giscus:
  repo:        mavrikant/mavrikant.github.io
  repo_id:     ""
  category:    Blog Comments
  category_id: ""

# Add your google-analytics ID here to activate google analytics
google_analytics:   G-YDEV31NZ5J

# Add your Sentry Loader Script ID here to activate Sentry
# Found in Sentry: Settings → Projects → <project> → Client Keys (DSN) → Loader Script
sentry_loader_id:   d9df60c4bcc41adb14bb648db4013b52

# Add your Plausible site snippet ID here to activate Plausible Analytics
# Found in Plausible: Site Settings → General → Site Installation (the pa-<ID> in the script src)
plausible_id:       BMAxUwrIK8Icg2HpLysj2

# Build settings
markdown:           kramdown
paginate:           10
paginate_path:      "/posts/page:num/"
plugins:
  - jekyll-feed
  - jekyll-paginate
  - jekyll-sitemap
  - jekyll-admin
  - jekyll-seo-tag
  - jekyll-email-protect
  - jekyll-spaceship
  - jekyll-favicon
  - jekyll-coffeescript

# jekyll-spaceship — override the MathJax loader so it no longer injects the
# compromised polyfill.io CDN (June 2024 supply-chain attack; see Qualys
# advisory). The plugin's default `src` array hardcodes
# https://polyfill.io/v3/polyfill.min.js, which it adds to the <head> of every
# page containing math. MathJax 3 needs no polyfill on any currently supported
# browser, so we keep only the jsDelivr-hosted MathJax bundle. The array fully
# replaces the gem default, removing the polyfill.io <script> from the build.
jekyll-spaceship:
  mathjax-processor:
    src:
      - https://cdn.jsdelivr.net/npm/mathjax@3/es5/tex-mml-chtml.js
